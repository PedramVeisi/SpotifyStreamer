<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivityFragment">

    <EditText
        android:id="@+id/search_artist_edit_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:hint="@string/search_for_an_artist"
        android:singleLine="true" />

    <ListView
        android:id="@+id/artist_search_result_listview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/search_artist_edit_text"
        android:layout_centerHorizontal="true"
        android:divider="@android:color/transparent"
        android:dividerHeight="@dimen/artist_list_divider_height" />

    <!-- Is it good practice to put this here or should it go to a seprate file? -->
    <LinearLayout
        android:id="@+id/empty_list_message_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone">

        <ImageView
            android:id="@+id/imageView"
            android:layout_width="@dimen/artist_empty_view_image_width"
            android:layout_height="@dimen/artist_empty_view_image_height"
            android:layout_gravity="center_horizontal"
            android:alpha="0.2"
            android:src="@drawable/no_artist_found" />

        <TextView
            android:id="@+id/empty_list_message_textview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="@string/no_artist_found"
            android:textAppearance="?android:attr/textAppearanceMedium"
            android:textColor="@android:color/darker_gray" />

    </LinearLayout>

</RelativeLayout>